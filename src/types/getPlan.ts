export interface getPlan {
  planId: string;
  planName: string;
  mainImg: string;
  startDate: string;
  endDate: string;
  data: string;
  userId: number;
  createdAt: string;
  updatedAt: string;
}
